{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Infrastructure for an HTTPS static site using S3 and CloudFront",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "deploy:dev": "cdk deploy -c subdomain=beta --require-approval never",
    "deploy": "cdk deploy --require-approval never"
  },
  "author": {
    "name": "Amazon Web Services",
    "url": "https://aws.amazon.com",
    "organization": true
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^10.17.0",
    "aws-cdk": "^2.37.1",
    "typescript": "~4.6.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.1.0",
    "constructs": "^10.0.0"
  }
}
