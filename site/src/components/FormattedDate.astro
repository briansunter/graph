---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="ui" datetime={date?.toString()}>
	{
		date?.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
